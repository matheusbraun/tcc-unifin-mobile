import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  image: {
    width: 54,
    height: 54,
    borderWidth: 4,
    borderRadius: 4,
    borderColor: '#FFF',
  },
});

export default styles;
